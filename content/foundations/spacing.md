---
title: Spacing
description: Spacing defines the scale used to separate, group, and align interface elements in Scribe. Use it to keep layouts consistent across dense product workflows.
route: /foundations/spacing
category: foundations
---

Use the spacing scale to establish rhythm across product workflows. Choose a scale value to group related controls, separate unrelated groups, and align repeated rows; do not introduce arbitrary local values to correct an isolated alignment issue.

## Scale

The application spacing scale is: `xxxs` 2px, `xxs` 4px, `xs` 8px, `s` 12px, `m` 16px, `l` 20px, `xl` 32px, `xxl` 48px, and `xxxl` 64px.

## Usage

- Use `xxxs` through `s` for tight relationships inside a control, compact row, metadata group, or status treatment.
- Use `m` and `l` for standard control padding, related field groups, and normal gaps between repeated UI elements.
- Use `xl` and above to separate distinct sections, major page regions, or independent workflow groups.

Dense product interfaces should remain compact but legible. Apply larger gaps to clarify a change in task or hierarchy, not simply to make a surface feel decorative.
